<!-- AppCard.xaml - 标准卡片控件 -->
<UserControl
    x:Class="ScrewDriver.Toolbox.Controls.AppCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:ScrewDriver.Toolbox.Controls">
    <Border
        Background="{ThemeResource CardBackground}"
        CornerRadius="12"
        Padding="16"
        Width="280"
        Height="140">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0"
                       Text="{x:Bind Title, Mode=OneWay}"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorPrimary}"
                       TextTrimming="CharacterEllipsis"/>

            <TextBlock x:Name="DescText" Grid.Row="1"
                       Text="{x:Bind Description, Mode=OneWay}"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondary}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>

            <Button x:Name="ActionButton" Grid.Row="2"
                    Content="{x:Bind ActionText, Mode=OneWay}"
                    HorizontalAlignment="Right"
                    Click="OnActionClick"
                    Style="{StaticResource AccentButtonStyle}"/>
        </Grid>
    </Border>
</UserControl>
