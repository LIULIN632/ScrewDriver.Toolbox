<!-- GlobalStyles.xaml - 全局样式和主题 -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 主色 #0F6CBD -->
    <Color x:Key="AccentColor">#0F6CBD</Color>
    <Color x:Key="AccentColorDark">#0A4D8C</Color>
    <Color x:Key="SuccessColor">#16A34A</Color>
    <Color x:Key="WarningColor">#F59E0B</Color>
    <Color x:Key="ErrorColor">#DC2626</Color>
    <Color x:Key="InfoColor">#2563EB</Color>

    <!-- 浅色主题色 -->
    <Color x:Key="LightBackground">#F5F7FA</Color>
    <Color x:Key="LightCardBackground">#FFFFFF</Color>

    <!-- 深色主题色 -->
    <Color x:Key="DarkBackground">#1E1E1E</Color>
    <Color x:Key="DarkCardBackground">#2A2A2A</Color>

    <!-- 卡片背景刷 -->
    <SolidColorBrush x:Key="CardBackground" Color="{ThemeResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="PageHeaderBackground" Color="{ThemeResource PageHeaderBackgroundColor}" />

    <!-- 页面头部样式 -->
    <Style x:Key="PageHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <Style x:Key="PageSubHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondary}"/>
        <Setter Property="Margin" Value="0,0,0,24"/>
    </Style>

    <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,16,0,12"/>
    </Style>

    <!-- 卡片容器 -->
    <Style x:Key="CardGridStyle" TargetType="ItemsWrapGrid">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="ItemWidth" Value="280"/>
        <Setter Property="ItemHeight" Value="140"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <!-- 危险操作按钮 -->
    <Style x:Key="DangerButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#DC2626"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="CornerRadius" Value="6"/>
    </Style>

    <!-- 标准按钮样式（替代 WinUI 默认的 AccentButtonStyle） -->
    <Style x:Key="AccentButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource SystemAccentColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- 搜索框样式 -->
    <Style x:Key="SearchBoxStyle" TargetType="TextBox">
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="PlaceholderText" Value="搜索工具、设置、场景..."/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontSize" Value="16"/>
    </Style>

</ResourceDictionary>
