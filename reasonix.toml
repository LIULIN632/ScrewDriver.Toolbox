[permissions]
allow = ["Bash(python -c \"\nimport re\nfor fn in ['Colors.xaml', 'LightTheme.xaml', 'DarkTheme.xaml']:\n    with open(r'D:\\AICAN\\ScrewDriver.Toolbox\\src\\ScrewDriver.Toolbox.UI\\Themes\\\\' + fn, 'r', encoding='utf-8') as f:\n        content = f.read()\n    keys = re.findall(r'x:Key=\\\"([^\\\"]+)\\\"', content)\n    brush_keys = [k for k in keys if 'Brush' in k or 'Color' in k or 'Text' in k or 'Background' in k or 'Border' in k or 'Toggle' in k or 'Scroll' in k or 'Sidebar' in k]\n    print(f'{fn}: {len(brush_keys)} brush/resource keys')\n    if fn != 'Colors.xaml':\n        # Check for missing keys compared to Colors.xaml\n        colors_keys = set(re.findall(r'x:Key=\\\"([^\\\"]+)\\\"', content))\n        print(f'  Total keys: {len(colors_keys)}')\n\" 2>&1)"]
